{
  "name": "server",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "concurrently": "^3.5.1",
    "express": "^4.16.2",
    "moment": "^2.21.0"
  },
  "scripts": {
    "start": "node server/index.js",
    "start:dev": "NODE_ENV='development' concurrently 'yarn start' 'cd client && yarn start:react'",
    "build:react": "cd client && yarn build:react"
  }
}
